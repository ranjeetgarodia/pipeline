pipeline {
 agent any
 stages {
  stage('SCM Checkout') {
   git 'https://github.com/pranikita/maven-project-1'
  }
 } {
  stage('Compile Stage') {
   steps {
    withMaven(maven: 'local_mvn') {
     sh 'mvn clean compile'
    }
   }
  }

  stage('Testing Stage') {
   steps {
    withMaven(maven: 'local_mvn') {
     sh 'mvn test'
    }
   }
  }

  stage('install Stage') {
   steps {
    withMaven(maven: 'local_mvn') {
     sh 'mvn install'
    }
   }
  }

  stage('deploy to tomcat') {
   steps {
    sshagent(['app_pipeline']) {
     sh 'scp -o StrictHostKeyChecking=no */target/*.war ec2-user@3.210.201.230:/var/lib/tomcat/webapps'
    }
   }
  }
 }
}
